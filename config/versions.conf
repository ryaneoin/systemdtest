# ── nm2-ingest binary versions ────────────────────────────────────────────────
# Bump a version here and re-run install.sh <env> to upgrade that component.
# install.sh skips downloads when the installed version already matches.

TELEGRAF_VERSION=1.33.0
VMAGENT_VERSION=1.106.1      # pulled from vmutils-prod OSS release
PROMETHEUS_VERSION=2.55.1
LOKI_VERSION=3.3.2
